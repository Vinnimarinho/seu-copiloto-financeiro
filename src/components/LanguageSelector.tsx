/**
 * Seletor de idioma — temporariamente DESATIVADO para o lançamento PT-BR oficial.
 *
 * O produto está sendo lançado apenas em português do Brasil. EN/ES voltam quando
 * a internacionalização estiver completa nas telas principais (não apenas nas
 * chaves declaradas no JSON). Mantemos o componente para reativação rápida:
 * basta exportar o conteúdo abaixo no lugar do null.
 */
export function LanguageSelector() {
  return null;
}
